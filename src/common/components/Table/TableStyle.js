import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  userInfo: {
    display: "flex",
    flexDirection: "row",
    justifyContent: "space-between",
  },
  userName: {
    width: "75%",
  },
  img: {
    border: 1,
    borderRadius: "50%",
  },

});